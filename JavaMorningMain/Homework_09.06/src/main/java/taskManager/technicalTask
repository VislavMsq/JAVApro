Вам предстоит создать простую систему управления задачами (TODO-list), используя несколько классов и коллекции.
В задании включены следующие требования:
1) Создайте класс taskManager.Task, представляющий задачу.
    У задачи должны быть следующие свойства:
        ++1.Заголовок (title) - строковое значение, описывающее задачу.
        ++2.Описание (description) - строковое значение, дополнительная информация о задаче.
        ++3.Статус выполнения (completed) - логическое значение, указывающее, выполнена ли задача.
2) Создайте класс taskManager.TaskManager, который будет представлять менеджер задач.
    У taskManager.TaskManager должны быть следующие методы:
        ++1.addTask(taskManager.Task task): добавляет задачу в список задач.
        ++2.removeTask(taskManager.Task task): удаляет задачу из списка задач.
        ++3.getAllTasks(): возвращает список всех задач.
        ++4.getCompletedTasks(): возвращает список выполненных задач.
        ++5.getIncompleteTasks(): возвращает список невыполненных задач.
3)Напишите код, чтобы создать несколько экземпляров класса taskManager.Task и добавить их в taskManager.TaskManager.
        1.Используя циклы (например, цикл for-each), переберите список задач в taskManager.TaskManager и выведите информацию
          о каждой задаче, включая заголовок, описание и статус выполнения.
        2.Создайте новый список (List) и перенесите некоторые задачи из taskManager.TaskManager в этот список. Убедитесь,
          что задачи успешно перенесены и отображаются в новом списке.
        3.Создайте новый массив (Array) и перенесите некоторые задачи из нового списка в этот массив. Убедитесь,
          что задачи успешно перенесены и отображаются в новом массиве.
4)Выведите информацию о задачах в новом массиве, используя циклы.
5)Запустите программу и убедитесь, что вывод содержит правильные данные и отражает состояние задач (выполнена или невыполнена).
Примечание: Вы можете использовать классы ArrayList или LinkedList для хранения задач в taskManager.TaskManager и для создания новых списков.
    Также вы можете использовать массивы для хранения задач в новом массиве.